/// @description 

// Inherit the parent event
event_inherited();

bh_ui_type = BH_UI_CIRCLE;

ability = 0;
usable = true;

hotkey = global.hotkey_ability_one;
text_xOffset = (sprite_width * 0.25);
text_yOffset = (sprite_height * 0.25);