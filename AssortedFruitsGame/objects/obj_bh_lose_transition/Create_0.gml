/// @description 

// Inherit the parent event
event_inherited();

x = global.resolution_w / 2;
y = global.resolution_h / 2;
sprite_set_speed(sprite_index, 3, spritespeed_framespersecond);

image_index = BH_PLAYER_HEALTH_DEFAULT;
image_speed = 1;

